import 'package:flutter/material.dart';

class RiderSetting extends StatelessWidget {
  const RiderSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("rider Setting"),
      ),
    );
  }
}