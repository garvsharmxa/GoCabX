import 'package:flutter/material.dart';

class RidebookPage extends StatelessWidget {
  const RidebookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ride Book Page"),
      ),
    );
  }
}